name: style_sense_sl
description: Hyper-niche fashion discovery MVP
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  provider: ^6.1.2
  
  # UI & Media (Web-compatible only)
  flutter_staggered_grid_view: ^0.7.0
  shimmer: ^3.0.0
  
  # Utilities (Web-compatible)
  uuid: ^4.5.1
  url_launcher: ^6.3.2
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.0.5
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
